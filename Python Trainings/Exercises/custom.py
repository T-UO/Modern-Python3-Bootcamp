# every Python file has a __name__variable
# if the file is the main file being run, its value is "__main__"
# Otherwise, its value is the file name
# the __name__ variable allows you to check when the file is executed directly or imported as a module